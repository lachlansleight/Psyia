// We need to manually mark what our kernel names are
#pragma kernel EmitFunction

AppendStructuredBuffer<uint> deadList;

[numthreads(1024,1,1)]
void EmitFunction(uint3 id : SV_DispatchThreadID) {
	deadList.Append(id.x); 
}